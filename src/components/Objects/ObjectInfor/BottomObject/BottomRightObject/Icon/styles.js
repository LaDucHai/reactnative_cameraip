import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    page: {
    },
    icon: {
        margin: 5,
        alignItems: 'center',
    },
    iconText: {
        color: 'white'
    }
});

export default styles;