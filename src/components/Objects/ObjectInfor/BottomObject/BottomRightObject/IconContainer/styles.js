import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    page: {},
    icon: {
        margin: 3,
        alignItems: 'center',
    },
    iconText: {
        color: 'white'
    }
});

export default styles;