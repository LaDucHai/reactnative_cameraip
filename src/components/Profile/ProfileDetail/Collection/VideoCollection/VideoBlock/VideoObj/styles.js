import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    page: {},
    image: {
        height: 200
    }, 
    iconContainer: {
        position: 'absolute',
        alignItems: 'center',
        width: '100%',
        height: '100%'
    },
    icon: {
        height: '100%',
        alignItems: 'center',
        flexDirection: 'row'
    }
});

export default styles;