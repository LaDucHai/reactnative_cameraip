import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    page: {
        alignItems: 'center',
        marginRight: 15,
        marginLeft: 15
    }, 
    textView: {
        width: 70,
        alignItems: 'center'
    }
});

export default styles;